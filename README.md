🎵 Mi Repertorio - Laura González
Aplicación CRUD desarrollada con Node.js y Express para la gestión de repertorios musicales.

🚀 Instrucciones de Inicio
Sigue estos pasos para ejecutar el proyecto localmente tras clonar el repositorio:

1. Instalación de Dependencias
Ejecuta el siguiente comando para instalar Express y Nodemon (configurados en el package.json):

**npm install**

3. Inicialización del Almacenamiento
Asegúrate de que el archivo repertorio.json exista en la raíz del directorio con la siguiente estructura inicial:

**JSON
[]**

3. Ejecución del Proyecto
El proyecto cuenta con dos scripts de ejecución:

**npm run dev**

📁 Estructura del Proyecto
index.js: Servidor Express y definición de rutas API.

index.html: Interfaz de usuario y lógica de consumo de API.

repertorio.json: Archivo de persistencia de datos local.

.gitignore: Exclusión de carpetas de dependencias.
